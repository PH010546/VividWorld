# 更新紀錄

*[English](CHANGELOG.en.md)*

每一版都會寫清楚三件事：

- **這次改了什麼**
- **舊戰役能不能直接繼續玩**
- **你的設定檔要不要自己動手**——更新只會幫你補上新的設定項，你已經有的值不會被改掉

## 尚未發佈

## v0.9.2

- **新增「傳聞模式」（自動／暢玩／寫實），新角色也聽得到傳聞了**：以前領主要對你好感度 30 以上才會主動提起傳聞，
  新角色（好感度都在 0～9）幾乎一句都聽不到。現在分兩層：好感度到 30 照舊講完整的版本；不到 30 但到「閒聊門檻」的人會講個大概
  （消息多轉了兩手、細節少一些）。閒聊門檻暢玩是 0（不討厭你就可能講）、寫實是 10。
  暢玩模式下，裝了 Not-a-Noble、Lowborn 這類平民身分模組的玩家，家族等級 0 也能開口打聽；寫實模式照舊要到等級 1。
  預設是「自動」：偵測到平民身分模組就用寫實，否則用暢玩；第一次（或判定結果改變時）跳一個視窗說明，之後每次讀檔左下角有一行淺綠色的提示。
  MCM「對話」組最上面就是這一格，改了馬上生效、不必重新讀檔。
  實測同一局新角色：暢玩下主動講的人從 0 變成 440 人（全是「大概」）。每天最多一位主動講、同一人 3 天冷卻、打聽時肯不肯講的判定都沒變。
  舊戰役可以直接繼續玩。設定檔不必動：會自動補上 `dialogue.volunteerMode`（`auto`）、`casualChatRelationGate`（0）、
  `realisticChatRelationGate`（10）、`gistExtraHops`（2）——**所以更新之後預設就是自動模式**，想維持舊的樣子（只有好感度 30 以上才講）請在 MCM 選「寫實」並把 `realisticChatRelationGate` 改成 30。
  MCM 原本的「平民身分相容模式」那一格拿掉了（`commonerCompatMode` 仍留在 `config.json`、照舊有效）；
  「主動分享好感度門檻」改名「完整分享好感度門檻」。
- **轉述別人的消息時，不再說「事實上，當時我也在場」**：這句開頭現在只有當事人或目擊者重講時才會出現。舊戰役可以直接繼續玩，設定檔不必動。
- **問「最近路上有什麼消息嗎？」而對方答不出來時，會依原因回不同的話**：不願意跟你講、什麼都沒聽說、忘了、消息已經過時、你都已經聽過了，
  各有一句（以前不管什麼原因都是同一句）。誰肯講、誰不肯講的判定沒有改。舊戰役可以直接繼續玩，設定檔不必動。
- **開發者工具：量「玩家為什麼聽得少」**：每場對話記下主動講／打聽各卡在哪一道閘（好感度、冷卻、每日上限、沒東西講、被別的模組繞開……），
  按遊戲日加總存成戰役資料夾裡的 `listen_tally.json`，每天印一行摘要；讀檔時與每天另外預演一次「現在跟網裡每個人說話會怎樣」。
  開發者對話多兩行「(dev) 為什麼我聽得少？」「(dev) 現在跟每個人談會怎樣？」。舊戰役可以直接繼續玩：加總從更新後才開始累積。
  會自動補上 `debug.listenTally`（預設 `true`，設成 `false` 就不記）。
- **日誌不再帶查不到的內部編號**：`log.txt` 裡原本有十幾行附著 `ledger X-17` 這類只有開發者內部文件才查得到的編號，現在只留說明（在地化自檢那一行改成直接寫 OK／FAILED 與原因）。舊戰役可以直接繼續玩，設定檔不必動。

## v0.9.1

- **紀事視窗只列出你自己聽過的消息，而且每一則都看得到**：`Ctrl+L` 打開的紀事改讀一份「你聽過的消息」專屬紀錄，
  剛聽到的排在最上面；就算世界上已經沒有人記得那件事，你聽過的仍然留在紀事裡。
  舊戰役可以直接繼續玩：第一次載入時會自動從既有資料補齊你已經知道的消息。設定檔不必動。
- **紀事視窗裡的人名、地名可以點了**：名字會跟對話框裡一樣變色，點下去打開那個人或那個地方的百科頁；
  關掉百科後紀事視窗自己回來。舊戰役可以直接繼續玩，設定檔不必動
  （`presentation.encyclopediaLinksEnabled` 設成 `false` 時名字照舊是純文字、不能點）。
- **存檔時會清掉已經沒有人記得的舊消息**：一則消息如果世上已經沒有任何人記得，存檔時就會從模組的資料裡刪掉，
  長期遊玩時資料不會一直變大（實測一局 831 則刪掉 336 則）。你聽過的消息不受影響，紀事裡照樣看得到；
  秘密、恩怨、還在傳的消息都會留著。讀回更早的存檔時，當時的消息會跟著回來。
  舊戰役可以直接繼續玩：第一次載入時會自動重建一次索引。設定檔不必動。
- **長時間遊玩時，模組佔用的記憶體不再只增不減**：很久沒用到的舊消息資料會從記憶體放掉，要用時再從磁碟讀回來。
  另外，讀檔之後 NPC 輪流傳話時，不會再把輪次浪費在已經什麼都不記得的人身上（實測一局讀檔後 666 人減為 608 人）。
  舊戰役可以直接繼續玩，設定檔不必動（會自動補上新設定 `persistence.shardCacheIdleFlushes`，預設 8；設成 0 就永遠不放）。

## v0.9.0

第一個公開 beta。
